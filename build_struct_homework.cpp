//This code determines the probablity of success of drawing n black chips in a multi-color bag after n draws without replacement
#include<iostream>
#include<string>

using namespace std;

struct bags {
    int items;
    int draws; //equivalent to total items needed
    int blacks;

};

bool adequate_items (int items, int blacks) {
    return items > 2*blacks;

}

bool adequate_draws (int items, int draws) {
    return items > 2*draws;
}

int main(){

    int items, draws, blacks, num_bags_viewed, num_bags_assessed;
    struct          
   
    if(TRUE){
    num_bags_viewed = num_bags_assessed = 0
       while(TRUE){


      if (adequate_items(items,blacks) && adequate_draws(items,draws)) {
           cout << calculating;

          }   
       }       
    }
}
